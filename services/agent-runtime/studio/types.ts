export interface Trace { at: number; callId?: string; kind: string; name?: string; args?: unknown; result?: unknown; error?: string; code?: string; durationMs?: number; event?: unknown }
export interface Run { id: string; cursor?: number; prompt: string; mode: 'live' | 'scripted'; started: number; ended?: number; status: 'running' | 'completed' | 'failed' | 'cancelled' | 'interrupted'; answer: string; traces: Trace[]; tokens: number; cost: number; review?: { verdict: string; note: string }; error?: string }
export interface AgentView { id: string; name: string; type: string; description: string; suggestions: string[]; tools: string[]; toolDefinitions?: { name: string; description: string; parameters: unknown }[]; systemPrompt?: string; connected: boolean; runs: Run[]; busy: boolean; scripted: boolean }
